const endDay = require('../src/jsonSimulateArray');

describe('Simulatiogn associative arrays', () => {
  test('should use as array correctly', () => {
    expect(endDay).toBe('Wednesday');
  });
});

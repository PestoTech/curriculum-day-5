// lowercaseKeys({FOO: true, bAr: false});
// => { foo: true, bar: false }

function lowercaseKeys(obj) {
    let newObj = {};
    for (let [k, v] of obj) {
        newObj[obj[i]]
    }

}

module.exports = lowercaseKeys;